const supportedLanguages = ["en", "es", "fr", "zh"]; const languageStorageKey = "timelessStoriesOfficialLanguage"; const themeStorageKey = "timelessStoriesColorMode"; const supportedThemes = ["system", "light", "dark"]; const previewRewardThresholdSeconds = 30; const mismatchFlipBackDelayMs = 1200; let linkRegistry = null; let localizedLinks = null; const board = document.getElementById("board"); const movesValue = document.getElementById("movesValue"); const matchesValue = document.getElementById("matchesValue"); const timeValue = document.getElementById("timeValue"); const matchInsight = document.getElementById("matchInsight"); const matchInsightTitle = document.getElementById("matchInsightTitle"); const matchInsightText = document.getElementById("matchInsightText"); const legendToast = document.getElementById("legendToast"); const legendToastTitle = document.getElementById("legendToastTitle"); const legendToastText = document.getElementById("legendToastText"); const legendToastClose = document.getElementById("legendToastClose"); const winPanel = document.getElementById("winPanel"); const winTitle = document.getElementById("winTitle"); const winMessage = document.getElementById("winMessage"); const legendsPanel = document.getElementById("legendsPanel"); const legendsPanelTitle = document.getElementById("legendsPanelTitle"); const legendsList = document.getElementById("legendsList"); const newGameButton = document.getElementById("newGameButton"); const shareButton = document.getElementById("shareButton"); const hintButton = document.getElementById("hintButton"); const topGetCopyButton = document.getElementById("topGetCopyButton"); const topViewLegendsButton = document.getElementById("topViewLegendsButton"); const playAgainButton = document.getElementById("playAgainButton"); const viewLegendsButton = document.getElementById("viewLegendsButton"); const winShareButton = document.getElementById("winShareButton"); const closeWinButton = document.getElementById("closeWinButton"); const closeLegendsButton = document.getElementById("closeLegendsButton"); const previewRewardButton = document.getElementById("previewRewardButton"); const getCopyButton = document.getElementById("getCopyButton"); const legendsGetCopyButton = document.getElementById("legendsGetCopyButton"); const searchParams = new URLSearchParams(window.location.search); const isEmbedded = searchParams.get("embed") === "1"; let memoryGame = { pairs: [] }; let firstCard = null; let secondCard = null; let lockBoard = false; let moves = 0; let matches = 0; let elapsedSeconds = 0; let timer = null; let toastTimer = null; let gameCompleted = false; let translations = null; let deck = []; function getStoredTheme() { try { const storedTheme = window.localStorage.getItem(themeStorageKey); return supportedThemes.includes(storedTheme) ? storedTheme : "system"; } catch (error) { return "system"; } } function applyTheme() { const theme = getStoredTheme(); const prefersDark = window.matchMedia?.("(prefers-color-scheme: dark)")?.matches; document.documentElement.dataset.theme = theme === "dark" || (theme === "system" && prefersDark) ? "dark" : "light"; } function getShareUrl() { const url = new URL(window.location.href); url.searchParams.delete("embed"); return url.href; } function getStoredLanguage() { try { return window.localStorage.getItem(languageStorageKey); } catch (error) { return null; } } function getLanguage() { const storedLanguage = getStoredLanguage(); const browserLanguage = ( navigator.languages ? navigator.languages[0] : navigator.language || "en" ).split("-")[0]; if (supportedLanguages.includes(storedLanguage)) { return storedLanguage; } return supportedLanguages.includes(browserLanguage) ? browserLanguage : "en"; } function getTranslationValue(obj, path) { return path.split(".").reduce((acc, part) => acc && acc[part], obj); } function mergeLinkConfig(defaults, overrides = {}) { const merged = { ...defaults }; Object.entries(overrides || {}).forEach(([key, value]) => { const defaultValue = merged[key]; const shouldMerge = value && typeof value === "object" && !Array.isArray(value) && defaultValue && typeof defaultValue === "object" && !Array.isArray(defaultValue); merged[key] = shouldMerge ? mergeLinkConfig(defaultValue, value) : value; }); return merged; } function resolveGameHref(href) { if (!href) { return ""; } if (/^https?:\/\//i.test(href)) { return href; } return getTranslationValue(localizedLinks, href) || ""; } async function loadTranslations() { const language = getLanguage(); document.documentElement.lang = language; try { const [translationResponse, linksResponse] = await Promise.all([ fetch(`js/i18n/lang-${language}.json`), fetch("js/data/links.min.json"), ]); const data = await translationResponse.json(); linkRegistry = await linksResponse.json(); localizedLinks = mergeLinkConfig( linkRegistry.localized.default, linkRegistry.localized[language], ); translations = data.translations; memoryGame = translations.memoryGame || { pairs: [] }; } catch (error) { translations = { mapLegends: {}, memoryGame: { pairs: [] } }; memoryGame = translations.memoryGame; localizedLinks = {}; } } function setText(selector, value) { const element = document.querySelector(selector); if (element && value !== undefined) { element.textContent = value; } } function setElementText(element, value) { if (element && value !== undefined) { element.textContent = value; } } function setElementHref(element, value) { if (element) { element.href = value || ""; } } function setElementHidden(element, shouldHide) { if (element) { element.hidden = shouldHide; element.toggleAttribute("hidden", shouldHide); } } function applyUiCopy() { document.title = memoryGame.title || document.title; document.querySelector("meta[name='description']").content = memoryGame.subtitle || ""; document.querySelector("meta[property='og:title']").content = memoryGame.title || ""; document.querySelector("meta[property='og:description']").content = memoryGame.subtitle || ""; document.querySelector("meta[property='og:url']").content = getShareUrl(); document.querySelector("meta[property='twitter:title']").content = memoryGame.title || ""; document.querySelector("meta[property='twitter:description']").content = memoryGame.subtitle || ""; document.querySelector("link[rel='canonical']").href = getShareUrl(); document.getElementById("gameTitle").textContent = memoryGame.title || ""; document.querySelector(".game-shell").setAttribute("aria-label", memoryGame.title || ""); setText(".game-subtitle", memoryGame.subtitle); newGameButton.textContent = memoryGame.newGame || ""; shareButton.textContent = memoryGame.share || ""; hintButton.textContent = memoryGame.hint || "Hint"; setElementText(topGetCopyButton, memoryGame.getCopy || "Get a copy of the book"); setElementHref(topGetCopyButton, resolveGameHref(memoryGame.getCopyHref)); setElementText(topViewLegendsButton, memoryGame.viewLegends || "View all legends"); playAgainButton.textContent = memoryGame.playAgain || ""; setElementText(viewLegendsButton, memoryGame.viewLegends || "View all legends"); winShareButton.textContent = memoryGame.share || ""; closeWinButton.setAttribute("aria-label", memoryGame.close || "Close"); closeLegendsButton?.setAttribute("aria-label", memoryGame.close || "Close"); legendToastClose.setAttribute("aria-label", memoryGame.close || "Close"); previewRewardButton.textContent = memoryGame.previewReward || "Preview the book"; previewRewardButton.href = resolveGameHref(memoryGame.previewRewardHref) || "https://bit.ly/4dyjiZz"; getCopyButton.textContent = memoryGame.getCopy || "Get a copy of the book"; getCopyButton.href = resolveGameHref(memoryGame.getCopyHref); setElementText(legendsGetCopyButton, memoryGame.getCopy || "Get a copy of the book"); setElementHref(legendsGetCopyButton, resolveGameHref(memoryGame.getCopyHref)); winTitle.textContent = memoryGame.winTitle || ""; setElementText(legendsPanelTitle, memoryGame.legendsTitle || ""); document.querySelector(".status-grid").setAttribute("aria-label", memoryGame.statusLabel || ""); setText('[data-ui-label="moves"]', memoryGame.moves); setText('[data-ui-label="matches"]', memoryGame.matches); setText('[data-ui-label="time"]', memoryGame.time); board.setAttribute("aria-label", memoryGame.boardLabel || ""); } function getLegendName(key) { return translations?.mapLegends?.[key]?.name || key; } function getLegendDescription(key) { return translations?.mapLegends?.[key]?.desc || ""; } function getOriginLabel(side) { return side === "source" ? translations?.country1 || "" : translations?.country2 || ""; } function getOriginFlag(side) { const label = getOriginLabel(side); return label.match(/[\u{1F1E6}-\u{1F1FF}]{2}/u)?.[0] || ""; } function getPairIcon(source, mirror) { const pairKey = [source, mirror].sort().join("|"); const pairIcons = { "blackKnight|caveOfSalamanca": "devil", "midnightWasherwomen|siguanaba": "masks", "dip|goodAndBadCadejo": "paw", "ghostOfSanGines|headlessPriest": "cross", "fleshlessWoman|girlOnTheCurve": "routes", "funeralCortegeOfChalchuapa|holyCompany": "candle", "fairJudgeOfTheNight|gaueko": "night", "ploranera|weepingWoman": "cry", "dwarf|trasgu": "wizard", "mulus|urco": "tomb", }; return pairIcons[pairKey] || "masks"; } function getPairCount() { return Array.isArray(memoryGame.pairs) ? memoryGame.pairs.length : 0; } function buildDeck() { const pairs = Array.isArray(memoryGame.pairs) ? memoryGame.pairs : []; return pairs.flatMap(({ source, mirror, insightTitle, insight }, pairIndex) => { const icon = getPairIcon(source, mirror); return [ { key: source, pairId: `pair-${pairIndex}`, side: "source", name: getLegendName(source), desc: getLegendDescription(source), icon, insightTitle, insight, }, { key: mirror, pairId: `pair-${pairIndex}`, side: "mirror", name: getLegendName(mirror), desc: getLegendDescription(mirror), icon, insightTitle, insight, }, ]; }); } function shuffle(items) { const shuffled = [...items]; for (let i = shuffled.length - 1; i > 0; i -= 1) { const j = Math.floor(Math.random() * (i + 1)); [shuffled[i], shuffled[j]] = [shuffled[j], shuffled[i]]; } return shuffled; } function getInitials(name) { return name .replace(/^The /, "") .split(" ") .slice(0, 2) .map((part) => part[0]) .join("") .toUpperCase(); } function formatTime(totalSeconds) { const minutes = Math.floor(totalSeconds / 60); const seconds = String(totalSeconds % 60).padStart(2, "0"); return `${minutes}:${seconds}`; } function updateStatus() { movesValue.textContent = moves; matchesValue.textContent = `${matches}/${getPairCount()}`; timeValue.textContent = formatTime(elapsedSeconds); } function startTimer() { if (timer) { return; } timer = window.setInterval(() => { elapsedSeconds += 1; updateStatus(); }, 1000); } function stopTimer() { if (!timer) { return; } window.clearInterval(timer); timer = null; } function createCard(character, index) { const card = document.createElement("button"); card.className = "card"; card.type = "button"; card.dataset.pairId = character.pairId; card.dataset.character = character.name; card.dataset.description = character.desc || ""; card.dataset.insightTitle = character.insightTitle || ""; card.dataset.insight = character.insight || ""; card.setAttribute("aria-label", memoryGame.hiddenCard || ""); card.innerHTML = ` <span class="card-inner"> <span class="card-face card-back" aria-hidden="true"> <span>TS</span> </span> <span class="card-face card-front"> <span class="character-mark"> <i class="character-icon icon-${character.icon}" aria-hidden="true"></i> <span class="character-flag" aria-hidden="true">${getOriginFlag(character.side)}</span> </span> <span class="character-title"> <span class="character-name">${character.name}</span> </span> <span class="character-origin">${getOriginLabel(character.side)}</span> </span> </span> `; card.addEventListener("click", () => flipCard(card)); card.dataset.index = index; return card; } function resetTurn() { firstCard = null; secondCard = null; lockBoard = false; } function hideOpenCards() { firstCard.classList.remove("is-flipped"); secondCard.classList.remove("is-flipped"); firstCard.setAttribute("aria-label", memoryGame.hiddenCard || ""); secondCard.setAttribute("aria-label", memoryGame.hiddenCard || ""); resetTurn(); } function markMatch() { firstCard.classList.add("is-matched"); secondCard.classList.add("is-matched"); matches += 1; showMatchInsight(firstCard); updateStatus(); if (matches === getPairCount()) { stopTimer(); gameCompleted = true; board.classList.add("is-complete"); setTopLegendActionVisibility(true); setPreviewRewardVisibility(elapsedSeconds < previewRewardThresholdSeconds); winMessage.textContent = (memoryGame.win || "") .replace("{pairs}", getPairCount()) .replace("{moves}", moves) .replace("{time}", formatTime(elapsedSeconds)); winPanel.classList.add("is-visible"); } resetTurn(); } function showMatchInsight(card) { matchInsightTitle.textContent = card.dataset.insightTitle || ""; matchInsightText.textContent = card.dataset.insight || ""; matchInsight.classList.toggle( "is-visible", Boolean(card.dataset.insightTitle || card.dataset.insight), ); } function createLegendCell(characterKey, side) { const cell = document.createElement("article"); cell.className = "legend-cell"; const name = getLegendName(characterKey); const description = getLegendDescription(characterKey); cell.innerHTML = ` <span class="legend-copy"> <strong>${name}</strong> <span>${getOriginLabel(side)}</span> <p>${description}</p> </span> `; return cell; } function renderLegendsList() { const pairs = Array.isArray(memoryGame.pairs) ? memoryGame.pairs : []; const rows = pairs.map(({ source, mirror, insightTitle, insight }) => { const row = document.createElement("section"); const icon = getPairIcon(source, mirror); row.className = "legend-row"; const category = document.createElement("header"); category.className = "legend-category"; category.innerHTML = ` <strong>${insightTitle || ""}</strong> <span>${insight || ""}</span> <span class="legend-mark"> <i class="character-icon icon-${icon}" aria-hidden="true"></i> </span> `; row.append( category, createLegendCell(source, "source"), createLegendCell(mirror, "mirror"), ); return row; }); legendsList?.replaceChildren(...rows); } function hideLegendToast() { legendToast.hidden = true; if (toastTimer) { window.clearTimeout(toastTimer); toastTimer = null; } } function showToast(title, text) { if (!text) { hideLegendToast(); return; } legendToastTitle.textContent = title || ""; legendToastText.textContent = text; legendToast.hidden = false; if (toastTimer) { window.clearTimeout(toastTimer); } toastTimer = window.setTimeout(hideLegendToast, 5200); } function showLegendToast(card) { showToast(card.dataset.character || "", card.dataset.description || ""); } function setPreviewRewardVisibility(shouldShow) { previewRewardButton.hidden = !shouldShow; previewRewardButton.toggleAttribute("hidden", !shouldShow); } function setTopLegendActionVisibility(shouldShow) { setElementHidden(topGetCopyButton, !shouldShow); setElementHidden(topViewLegendsButton, !shouldShow); } function flipCard(card) { if (card.classList.contains("is-matched")) { showMatchInsight(card); if (gameCompleted) { showLegendToast(card); } return; } if (lockBoard || card === firstCard) { return; } startTimer(); card.classList.add("is-flipped"); card.setAttribute("aria-label", card.dataset.character); if (!firstCard) { firstCard = card; return; } secondCard = card; lockBoard = true; moves += 1; updateStatus(); if (firstCard.dataset.pairId === secondCard.dataset.pairId) { markMatch(); return; } window.setTimeout(hideOpenCards, mismatchFlipBackDelayMs); } function newGame() { stopTimer(); firstCard = null; secondCard = null; lockBoard = false; moves = 0; matches = 0; elapsedSeconds = 0; gameCompleted = false; board.classList.remove("is-complete"); matchInsight.classList.remove("is-visible"); matchInsightTitle.textContent = ""; matchInsightText.textContent = ""; winPanel.classList.remove("is-visible"); legendsPanel?.classList.remove("is-visible"); setTopLegendActionVisibility(false); setPreviewRewardVisibility(false); hideLegendToast(); winMessage.textContent = ""; updateStatus(); deck = shuffle(buildDeck()); renderLegendsList(); board.replaceChildren(...deck.map(createCard)); } function openLegendsPanel() { if (!gameCompleted) { return; } legendsPanel?.classList.add("is-visible"); } async function shareGame(button = shareButton) { const shareData = { title: memoryGame.title || document.title, text: memoryGame.shareText || memoryGame.subtitle || "", url: getShareUrl(), }; if (navigator.share) { await navigator.share(shareData); return; } await navigator.clipboard?.writeText(shareData.url); const originalLabel = button.textContent || memoryGame.share || ""; button.textContent = memoryGame.shareCopied || originalLabel; window.setTimeout(() => { button.textContent = originalLabel; }, 1600); } newGameButton.addEventListener("click", newGame); shareButton.addEventListener("click", () => { shareGame(shareButton).catch(() => {}); }); hintButton.addEventListener("click", () => { showToast(memoryGame.hint || "Hint", memoryGame.hintText || "Pay attention to the icons."); }); winShareButton.addEventListener("click", () => { shareGame(winShareButton).catch(() => {}); }); closeWinButton.addEventListener("click", () => { winPanel.classList.remove("is-visible"); }); viewLegendsButton?.addEventListener("click", openLegendsPanel); topViewLegendsButton?.addEventListener("click", openLegendsPanel); closeLegendsButton?.addEventListener("click", () => { legendsPanel?.classList.remove("is-visible"); }); legendToastClose.addEventListener("click", hideLegendToast); playAgainButton.addEventListener("click", newGame); applyTheme(); document.documentElement.dataset.embed = String(isEmbedded); window.matchMedia?.("(prefers-color-scheme: dark)")?.addEventListener("change", applyTheme); loadTranslations().finally(() => { applyUiCopy(); newGame(); });