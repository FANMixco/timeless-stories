const supportedLang = ["en", "es", "fr", "zh"]; const i18nCacheVersion = "20260525-json-parse"; const languageStorageKey = "timelessStoriesOfficialLanguage"; const themeStorageKey = "timelessStoriesColorMode"; const supportedThemes = ["system", "light", "dark"]; let translations; let linkRegistry; let localizedLinks; const browserLang = ( navigator.languages ? navigator.languages[0] : navigator.language || navigator.userLanguage ).split("-")[0]; const storedLang = getStoredLanguage(); const lang = supportedLang.includes(storedLang) ? storedLang : supportedLang.includes(browserLang) ? browserLang : "en"; const bookCoverBasePath = "img/cover-colorized-v2-sm"; const localizedBookCoverLanguages = new Set(["es"]); document.documentElement.lang = lang; function getStoredLanguage() { try { return window.localStorage.getItem(languageStorageKey); } catch (error) { return null; } } function storeLanguage(language) { try { window.localStorage.setItem(languageStorageKey, language); } catch (error) { return; } } function getStoredTheme() { try { const storedTheme = window.localStorage.getItem(themeStorageKey); return supportedThemes.includes(storedTheme) ? storedTheme : "system"; } catch (error) { return "system"; } } function storeTheme(theme) { try { if (theme === "system") { window.localStorage.removeItem(themeStorageKey); return; } window.localStorage.setItem(themeStorageKey, theme); } catch (error) { return; } } function applyTheme(theme = getStoredTheme()) { const selectedTheme = supportedThemes.includes(theme) ? theme : "system"; if (selectedTheme === "dark" || selectedTheme === "light") { document.documentElement.dataset.theme = selectedTheme; return; } document.documentElement.removeAttribute("data-theme"); } function initOfficialLanguageSelector() { const languageSelect = document.getElementById("officialLanguageSelect"); if (!languageSelect) return; languageSelect.value = lang; languageSelect.addEventListener("change", () => { const selectedLanguage = languageSelect.value; if ( !supportedLang.includes(selectedLanguage) || selectedLanguage === lang ) { return; } storeLanguage(selectedLanguage); window.location.reload(); }); } function initThemeSelector() { const themeSelect = document.getElementById("themeModeSelect"); if (!themeSelect) return; const storedTheme = getStoredTheme(); themeSelect.value = storedTheme; applyTheme(storedTheme); themeSelect.addEventListener("change", () => { const selectedTheme = supportedThemes.includes(themeSelect.value) ? themeSelect.value : "system"; storeTheme(selectedTheme); applyTheme(selectedTheme); }); } async function fetchTranslationData(url) { try { const response = await fetch(url); return await response.json(); } catch (error) { console.error("Error loading JSON:", error); throw error; } } function setDeferredFrameSource(frameId, src) { const frame = document.getElementById(frameId); if (!frame || !src) return; const assignSource = () => { if (!frame.getAttribute("src")) { frame.setAttribute("src", src); } }; if (!("IntersectionObserver" in window)) { assignSource(); return; } const observer = new IntersectionObserver( (entries) => { entries.forEach((entry) => { if (entry.isIntersecting) { observer.disconnect(); assignSource(); } }); }, { rootMargin: "250px 0px" }, ); observer.observe(frame); } function updateLocalizedBookCover(language) { const webpSource = document.getElementById("bookCoverWebpSource"); const jpegSource = document.getElementById("bookCoverJpgSource"); const coverImage = document.getElementById("bookCoverImage"); if (!webpSource || !jpegSource || !coverImage) return; const suffix = localizedBookCoverLanguages.has(language) ? `-${language}` : ""; const jpegPath = `${bookCoverBasePath}${suffix}.jpg`; const webpPath = `${bookCoverBasePath}${suffix}.webp`; const altText = translations?.bookCoverAlt || translations?.introSM || coverImage.alt; webpSource.setAttribute("srcset", webpPath); jpegSource.setAttribute("srcset", jpegPath); coverImage.setAttribute("src", jpegPath); coverImage.setAttribute("alt", altText); } function getTranslationValue(obj, path) { return path.split(".").reduce((acc, part) => acc && acc[part], obj); } function mergeLinkConfig(defaults, overrides = {}) { const merged = { ...defaults }; Object.entries(overrides).forEach(([key, value]) => { const defaultValue = merged[key]; const shouldMerge = value && typeof value === "object" && !Array.isArray(value) && defaultValue && typeof defaultValue === "object" && !Array.isArray(defaultValue); merged[key] = shouldMerge ? mergeLinkConfig(defaultValue, value) : value; }); return merged; } function getLinkValue(path) { return getTranslationValue(linkRegistry, path) || "#"; } function getLocalizedLink(path) { return getTranslationValue(localizedLinks, path) || "#"; } function applyTranslations() { document.querySelectorAll("[data-translation]").forEach((item) => { const value = getTranslationValue(translations, item.dataset.translation); if (value !== undefined) { item.innerHTML = value; } }); } function getCarouselCards() { return Array.isArray(translations?.carouselCards) ? translations.carouselCards : []; } function resolveLocalizedHref(href) { if (!href) return "#"; return /^https?:\/\//i.test(href) ? href : getLocalizedLink(href); } function getMemoryGameLabel() { return translations?.memoryGameTitle || ""; } function getPreviousBooks() { const previousBooks = Array.isArray(translations?.previousBooks) ? translations.previousBooks : []; return [ ...previousBooks, { title: `<span>${getMemoryGameLabel()}</span>`, type: "memoryGame", }, ]; } function renderMemoryGameModalTitle() { const modalTitle = document.getElementById("memoryGameModalTitle"); const modalFrame = document.querySelector("#memoryGameModal iframe"); const label = getMemoryGameLabel(); if (modalTitle) { modalTitle.textContent = label; } if (modalFrame) { modalFrame.title = label; } } function getMassMediaCards() { return Array.isArray(translations?.massMediaCards) ? translations.massMediaCards : []; } function resolveMassMediaHref(href) { if (!href) return "#"; return /^https?:\/\//i.test(href) ? href : getLinkValue(href); } function getInstitutionalRecords() { return Array.isArray(translations?.institutionalRecords) ? translations.institutionalRecords : []; } function resolveInstitutionalRecordHref(href) { if (!href) return "#"; return /^https?:\/\//i.test(href) ? href : getLinkValue(href); } function getSpecialGreetings() { return Array.isArray(translations?.specialGreetings) ? translations.specialGreetings : []; } function getCulturalCollaborations() { return Array.isArray(translations?.culturalCollaborations) ? translations.culturalCollaborations : []; } function renderSpecialGreetings() { const container = document.getElementById("specialGreetingsCards"); if (!container) return; container.innerHTML = getSpecialGreetings() .filter((card) => card?.name || card?.role) .map((card) => { const content = ` <div class="flex-row d-flex"> <i class="${card.icon || "icon-pen"}" aria-hidden="true"></i> <div class="desc"> <p><span>${card.name || ""}</span> <br>${card.role || ""}</p> </div> </div>`; const attrs = [ card.id ? `id="${card.id}"` : "", `class="buttons flex-row d-flex${card.className ? ` ${card.className}` : ""}"`, ].filter(Boolean).join(" "); if (!card.href) { return `<div ${attrs}>${content}</div>`; } return ` <a ${attrs} href="${card.href}" target="_blank" rel="noreferrer noopener" >${content}</a>`; }) .join(""); } function renderCulturalCollaborations() { const container = document.getElementById("culturalCollaborationCards"); if (!container) return; container.innerHTML = getCulturalCollaborations() .map((card) => ` <div class="col-12 col-md-5 col-lg-4"> <div class="cultural-card"> <img src="${card.image || ""}" alt="${card.alt || card.name || ""}" loading="lazy" /> <div> <h3>${card.name || ""}</h3> <p>${card.description || ""}</p> </div> </div> </div>`) .join(""); } const availabilityDate = new Date(2026, 4, 1, 0, 0, 0); const postLaunchIntroDate = new Date(2026, 4, 2, 0, 0, 0); let availabilityCountdownInterval; function updateHeroIntroTranslation() { const heroIntro = document.getElementById("intro3"); if (!heroIntro) return; heroIntro.dataset.translation = Date.now() < postLaunchIntroDate.getTime() ? "launchHeroBefore" : "launchHeroAfter"; } function getCountdownText(key, fallback) { return translations?.[key] || fallback; } function updateAvailabilityCountdown() { const countdowns = document.querySelectorAll(".availability-countdown"); if (!countdowns.length) return; const remaining = availabilityDate.getTime() - Date.now(); const isAvailable = remaining <= 0; const safeRemaining = Math.max(remaining, 0); const dayMs = 24 * 60 * 60 * 1000; const hourMs = 60 * 60 * 1000; const minuteMs = 60 * 1000; const days = Math.floor(safeRemaining / dayMs); const hours = Math.floor((safeRemaining % dayMs) / hourMs); const minutes = Math.floor((safeRemaining % hourMs) / minuteMs); const seconds = Math.floor((safeRemaining % minuteMs) / 1000); countdowns.forEach((countdown) => { countdown.classList.toggle("is-available", isAvailable); const status = countdown.querySelector(".availability-countdown-status"); const timer = countdown.querySelector(".availability-countdown-timer"); if (status) { status.innerHTML = isAvailable ? getCountdownText("countdownAvailable", "Fully available now") : getCountdownText("countdownIntro", "Fully available in"); } if (timer) { timer.hidden = isAvailable; } [ ["days", days], ["hours", hours], ["minutes", minutes], ["seconds", seconds], ].forEach(([unit, value]) => { const item = countdown.querySelector(`[data-countdown-unit="${unit}"]`); if (item) { item.textContent = String(value).padStart(unit === "days" ? 1 : 2, "0"); } }); }); } function initAvailabilityCountdown() { if (availabilityCountdownInterval) { window.clearInterval(availabilityCountdownInterval); } if (!document.querySelector(".availability-countdown")) { return; } updateAvailabilityCountdown(); availabilityCountdownInterval = window.setInterval( updateAvailabilityCountdown, 1000, ); } function getItemsPerSlide() { if (window.innerWidth < 768) return 1; if (window.innerWidth < 992) return 2; return 3; } function renderPriceCarousel() { const carouselElement = document.getElementById("priceCarousel"); const carouselInner = document.getElementById("priceCarouselInner"); if (!carouselElement || !carouselInner) return; const itemsPerSlide = getItemsPerSlide(); const existingInstance = bootstrap.Carousel.getInstance(carouselElement); if (existingInstance) { existingInstance.dispose(); } const slides = []; const cards = getCarouselCards(); for (let i = 0; i < cards.length; i += itemsPerSlide) { const group = cards.slice(i, i + itemsPerSlide); slides.push(` <div class="carousel-item ${i === 0 ? "active" : ""}"> <div class="row justify-content-center"> ${group.map((card) => buildPriceCard(card, itemsPerSlide)).join("")} </div> </div> `); } function buildPriceCard(card, currentItemsPerSlide) { const colClass = currentItemsPerSlide === 1 ? "col-12" : currentItemsPerSlide === 2 ? "col-12 col-md-6" : "col-12 col-md-4"; const bottomClass = currentItemsPerSlide === 1 ? card.mobileBottomClass : card.desktopBottomClass; return ` <div class="${colClass}"> <div class="single-price no-padding"> <div class="price-top"> <h4>${card.title || ""}</h4> </div> <p>${card.description || ""}</p> <div class="price-bottom ${bottomClass}"> <div class="${card.leftClass}"> <span class="h1">${card.price || ""}</span> </div> <div class="${card.rightClass}"> <a href="${resolveLocalizedHref(card.href)}" target="_blank" rel="noopener noreferrer" class="primary-btn">${card.cta || ""}</a> </div> </div> </div> </div> `; } carouselInner.innerHTML = slides.join(""); if (typeof applyTranslations === "function") { applyTranslations(); } initAvailabilityCountdown(); new bootstrap.Carousel(carouselElement, { interval: false, ride: false, wrap: true, }); } function renderBooksCarousel() { const carouselElement = document.getElementById("booksCarousel"); const carouselInner = document.getElementById("booksCarouselInner"); if (!carouselElement || !carouselInner) return; const itemsPerSlide = getItemsPerSlide(); const existingInstance = bootstrap.Carousel.getInstance(carouselElement); if (existingInstance) { existingInstance.dispose(); } const slides = []; const books = getPreviousBooks(); for (let i = 0; i < books.length; i += itemsPerSlide) { const group = books.slice(i, i + itemsPerSlide); slides.push(` <div class="carousel-item ${i === 0 ? "active" : ""}"> <div class="row justify-content-center g-4"> ${group.map((card) => buildBookCard(card, itemsPerSlide)).join("")} </div> </div> `); } function buildBookCard(card, currentItemsPerSlide) { const colClass = currentItemsPerSlide === 1 ? "col-12" : currentItemsPerSlide === 2 ? "col-12 col-md-6" : "col-12 col-md-4"; const isMemoryGameCard = card.type === "memoryGame"; const linkAttributes = isMemoryGameCard ? `href="memory-game.html" data-bs-toggle="modal" data-bs-target="#memoryGameModal"` : `href="${getLinkValue(card.href)}" target="_blank" rel="noopener noreferrer"`; const iconClass = isMemoryGameCard ? "icon-gamepad" : "icon-download"; const extraClass = isMemoryGameCard ? " book-card-memory" : ""; return ` <div class="${colClass}"> <a class="book-card${extraClass} text-decoration-none d-block h-100" ${linkAttributes}> <div class="card h-100 text-center shadow-sm border-0"> <div class="card-body d-flex flex-column justify-content-center"> <i class="${iconClass} mb-3 fs-1" aria-hidden="true"></i> <p class="mb-0 fw-semibold">${card.title || ""}</p> </div> </div> </a> </div>`; } carouselInner.innerHTML = slides.join(""); if (typeof applyTranslations === "function") { applyTranslations(); } new bootstrap.Carousel(carouselElement, { interval: false, ride: false, wrap: true, }); } function renderMassMediaCarousel() { const carouselElement = document.getElementById("mMediaCarousel"); const carouselInner = document.getElementById("mMediaCarouselInner"); if (!carouselElement || !carouselInner) return; const itemsPerSlide = getItemsPerSlide(); const existingInstance = bootstrap.Carousel.getInstance(carouselElement); if (existingInstance) { existingInstance.dispose(); } const slides = []; const mediaCards = getMassMediaCards(); for (let i = 0; i < mediaCards.length; i += itemsPerSlide) { const group = mediaCards.slice(i, i + itemsPerSlide); slides.push(` <div class="carousel-item ${i === 0 ? "active" : ""}"> <div class="row justify-content-center g-4"> ${group.map((card) => buildMassMediaCard(card, itemsPerSlide)).join("")} </div> </div> `); } function buildMassMediaCard(card, currentItemsPerSlide) { const colClass = currentItemsPerSlide === 1 ? "col-12 mMedia-container" : currentItemsPerSlide === 2 ? "col-12 col-md-6 mMedia-container" : "col-12 col-md-4 mMedia-container"; const cardClass = currentItemsPerSlide === 1 ? "card mx-3" : "card"; const imageClass = currentItemsPerSlide === 1 ? "card-img-top img-fluid" : "card-img-top"; const bodyClass = currentItemsPerSlide === 1 ? "card-body text-center font-weight-bold" : "card-body"; return ` <div class="${colClass}"> <div class="${cardClass}"> <a target="_blank" rel="noopener noreferrer" href="${resolveMassMediaHref(card.href)}" class="text-decoration-none text-dark d-block" > <picture> <source type="image/webp" srcset="${card.imageBase}.webp" /> <source type="image/jpeg" srcset="${card.imageBase}.jpg" /> <img class="${imageClass}" alt="${card.alt}" src="${card.imageBase}.jpg" loading="lazy" /> </picture> <div class="${bodyClass}"> <p class="card-text">${card.title || ""}</p> </div> </a> </div> </div>`; } carouselInner.innerHTML = slides.join(""); if (typeof applyTranslations === "function") { applyTranslations(); } new bootstrap.Carousel(carouselElement, { interval: false, ride: false, wrap: true, }); } function renderInstitutionalRecords() { const carouselElement = document.getElementById( "institutionalRecordsCarousel", ); const carouselInner = document.getElementById( "institutionalRecordsCarouselInner", ); if (!carouselElement || !carouselInner) return; const itemsPerSlide = getItemsPerSlide(); const existingInstance = bootstrap.Carousel.getInstance(carouselElement); if (existingInstance) { existingInstance.dispose(); } const slides = []; const records = getInstitutionalRecords(); for (let i = 0; i < records.length; i += itemsPerSlide) { const group = records.slice(i, i + itemsPerSlide); slides.push(` <div class="carousel-item ${i === 0 ? "active" : ""}"> <div class="row justify-content-center g-4"> ${group.map((card) => buildInstitutionalRecordCard(card, itemsPerSlide)).join("")} </div> </div> `); } function buildInstitutionalRecordCard(card, currentItemsPerSlide) { const colClass = currentItemsPerSlide === 1 ? "col-12 institutional-record-slide" : currentItemsPerSlide === 2 ? "col-12 col-md-6 institutional-record-slide" : "col-12 col-md-4 institutional-record-slide"; const institution = card.institution || ""; const title = card.title || ""; return ` <div class="${colClass}"> <article class="institutional-record-card"> <p class="institutional-record-country">${card.country || ""}</p> <h3>${institution}</h3> <p class="institutional-record-title">${title}</p> <p class="institutional-record-date">${card.date || ""}</p> <a href="${resolveInstitutionalRecordHref(card.href)}" class="institutional-record-link" target="_blank" rel="noopener noreferrer" >${card.linkLabel || ""}</a> </article> </div> `; } carouselInner.innerHTML = slides.join(""); if (typeof applyTranslations === "function") { applyTranslations(); } new bootstrap.Carousel(carouselElement, { interval: false, ride: false, wrap: true, }); } let currentCarouselMode = getItemsPerSlide(); window.addEventListener("resize", () => { const newMode = getItemsPerSlide(); if (newMode !== currentCarouselMode) { currentCarouselMode = newMode; renderPriceCarousel(); renderBooksCarousel(); renderMassMediaCarousel(); renderInstitutionalRecords(); } }); Promise.all([ fetchTranslationData(`js/i18n/lang-${lang}.min.json?v=${i18nCacheVersion}`), fetchTranslationData("js/data/links.min.json"), ]) .then(([translationData, linksData]) => { translations = translationData.translations; linkRegistry = linksData; localizedLinks = mergeLinkConfig( linkRegistry.localized.default, linkRegistry.localized[lang], ); initOfficialLanguageSelector(); initThemeSelector(); document.title = translations.title; updateLocalizedBookCover(lang); renderSpecialGreetings(); setDeferredFrameSource( "bookPreviewFrame", `https://leer.amazon.es/kp/card?asin=${localizedLinks.book}&preview=inline&linkCode=kpe&ref_=cm_sw_r_kb_dp_HJ6YDMXY6BRE1FA9AWE3`, ); setDeferredFrameSource( "preziPreviewFrame", `https://prezi.com/p/embed/${localizedLinks.prezi}`, ); const contactModal = document.getElementById("mContactUs"); renderCulturalCollaborations(); renderMemoryGameModalTitle(); renderPriceCarousel(); renderBooksCarousel(); renderMassMediaCarousel(); renderInstitutionalRecords(); applyTranslations(); contactModal.addEventListener( "show.bs.modal", () => { const cuScriptExist = document.getElementById("cu_script"); if (!cuScriptExist) { const script = document.createElement("script"); script.src = "https://www.cognitoforms.com/f/seamless.js"; script.id = "cu_script"; script.async = true; script.dataset.key = localizedLinks.contactUs.key; script.dataset.form = localizedLinks.contactUs.form; document.getElementById("divContactUs").appendChild(script); } }, { once: true }, ); window.dispatchEvent( new CustomEvent("translationsLoaded", { detail: { lang, translations }, }), ); }) .catch((e) => { console.error(e); });